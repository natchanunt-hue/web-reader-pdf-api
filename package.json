{
  "name": "cmd-pdf-generator",
  "version": "1.0.0",
  "description": "PDF generation API using Puppeteer for Render (Final Font Fix)", 
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "install:dependencies": "npm install && sudo apt-get update && sudo apt-get install -y fonts-thai-tlwg",
    "build": "echo 'Building Render Node Service...'"
  },
  "dependencies": {
    "@sparticuz/chromium": "^123.0.1",  
    "puppeteer-core": "^22.0.0",
    "express": "^4.18.2",
    "cors": "^2.8.5"
  }
}